name: Build and Test

on:
 push:
  branches: [main]
 pull_request:
  branches: [main]

permissions:
 contents: read

jobs:
 build-and-test:
  runs-on: ubuntu-latest

  steps:
   - uses: actions/checkout@v3

   - name: Setup .NET
     uses: actions/setup-dotnet@v3
     with:
      dotnet-version: 10.0

   - name: Build (Debug)
     run: dotnet build RDCore.slnx --configuration Debug

   - name: Test (Debug)
     run: dotnet test RDCore.slnx --configuration Debug --no-build --verbosity normal

   - name: Build (Release)
     run: dotnet build RDCore.slnx --configuration Release

   - name: Test (Release)
     run: dotnet test RDCore.slnx --configuration Release --no-build --verbosity normal
